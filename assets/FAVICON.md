# Site favicon

Drop the final favicon into this directory with the exact filename `favicon.png`.
Every HTML page links to `assets/favicon.png`, and the local preview server allows
that file. A square 512 × 512 PNG is recommended as the source image.
